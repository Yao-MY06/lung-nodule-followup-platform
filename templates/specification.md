# 规格说明：{{特性名称}}

> 本文件是本次工作的事实源。实现计划、任务和代码不得在没有明确决策的情况下改变这里的范围或验收标准。

## 1. 预期成果

`{{完成后用户、系统或运维人员能观察到什么}}`

## 2. 范围

**范围内**

- `{{能力、模块或接口 1}}`
- `{{能力、模块或接口 2}}`

**范围外**

- `{{明确不做的内容}}`

## 3. 约束与已有决策

- 技术约束：`{{语言、框架、兼容版本或性能目标}}`
- 安全/合规约束：`{{权限、PII、审计或保留要求}}`
- 产品/架构决策：`{{已批准的决策及来源}}`
- 不可改变的不变量：`{{规则}}`

## 4. 参与者与数据流

| 参与者/组件 | 输入 | 处理 | 输出 | 失败行为 |
| --- | --- | --- | --- | --- |
| `{{名称}}` | `{{输入}}` | `{{处理}}` | `{{输出}}` | `{{失败行为}}` |

## 5. 行为与边界情况

| 场景 | 预期行为 | 验证方式 |
| --- | --- | --- |
| `{{正常路径}}` | `{{结果}}` | `{{测试或手工步骤}}` |
| `{{空值/重复/超时/权限不足}}` | `{{结果}}` | `{{测试或手工步骤}}` |

## 6. 任务分解

每项任务必须可独立验证，并标明依赖。测试或验证条件先于实现任务确定。

| 编号 | 任务 | 依赖 | 输出/证据 | 负责人 |
| --- | --- | --- | --- | --- |
| `{{T1}}` | `{{单一可验证任务}}` | `{{无或编号}}` | `{{测试、日志或截图}}` | `{{姓名或 Worker}}` |

## 7. 验证标准

- [ ] `{{功能验收标准}}`
- [ ] `{{质量、性能或兼容性标准}}`
- [ ] `{{安全和权限标准}}`
- [ ] `{{回归测试标准}}`

## 8. 规格批准

- 规格审阅者：`{{姓名或角色}}`
- 批准日期：`{{YYYY-MM-DD}}`
- 变更记录：`{{批准后修改的内容和原因}}`

