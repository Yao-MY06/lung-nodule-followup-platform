# 实现任务 {{编号}}：{{单一结果}}

> 一个任务只负责一个可验证结果。完成前不能用“顺便重构”扩大范围。

## 输入与前置条件

- 来源计划：`{{implementation-plan.md 路径与任务编号}}`
- 前置任务：`{{编号或“无”}}`
- 目标：`{{完成后可观察的单一结果}}`
- 相关文件/符号：`{{确切路径和名称}}`
- 风险车道：`{{绿 / 黄 / 红}}`

## 先定义验证

**测试或验证场景**

- [ ] `{{正常路径：输入、动作、预期结果}}`
- [ ] `{{边界/失败路径：输入、动作、预期结果}}`

验证先决条件：`{{测试数据、环境、权限或截图要求}}`

## 实现约束

- 必须复用的既有模式：`{{文件、函数或接口}}`
- 不得改变：`{{不变量、公共行为或范围外内容}}`
- 依赖变更：`{{无 / 包名、官方来源和版本理由}}`

## 实现记录

`{{记录实际采用的方案、关键假设和与计划的偏差}}`

## 证据

- 变更 diff：`{{链接或提交}}`
- 测试/检查命令：`{{命令}}`
- 结果：`{{通过/失败及原因}}`
- 其他证据：`{{日志、截图或构建产物}}`

## 完成签核

- [ ] 我理解本任务 diff 的每一行。
- [ ] 验证条件由人或独立流程定义，且结果已记录。
- [ ] 任务没有引入未批准的范围或凭据。
- 实现者：`{{姓名}}`
- 审阅者：`{{姓名或角色}}`
- 完成日期：`{{YYYY-MM-DD}}`

