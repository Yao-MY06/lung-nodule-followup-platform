# 实现计划：{{特性名称}}

- 来源规格：`{{specification.md 路径}}`
- 任务简报：`{{task-brief.md 路径}}`
- 风险车道：`{{绿 / 黄 / 红}}`
- 计划作者：`{{姓名或代理}}`
- 日期：`{{YYYY-MM-DD}}`

## 1. 技术方案

`{{采用的既有模式、模块边界和关键接口；说明为什么符合仓库约定}}`

## 2. 文件变更表

| 文件 | 操作 | 责任 | 验证方式 |
| --- | --- | --- | --- |
| `{{路径}}` | `{{创建/修改/删除}}` | `{{变更内容}}` | `{{测试或检查}}` |

## 3. 依赖顺序

1. `{{任务 1：先定义测试、契约或数据结构}}`
2. `{{任务 2：实现最小行为}}`
3. `{{任务 3：集成、回归和清理}}`

阻塞关系：`{{任务编号之间的依赖}}`

## 4. 测试优先策略

- 先写/确认的测试或验证：`{{测试文件、场景或手工证据}}`
- AI 可实现的范围：`{{明确边界}}`
- 独立审查或验证方式：`{{人员、工具或流水线}}`

## 5. 发布、回滚与数据

- 发布步骤：`{{顺序、开关或迁移}}`
- 观测信号：`{{日志、指标、告警}}`
- 回滚条件：`{{触发条件}}`
- 回滚步骤：`{{命令或操作}}`
- 数据兼容/清理：`{{方案或“无”}}`

## 6. 风险与并行边界

| 风险 | 预防/验证 | 负责人 |
| --- | --- | --- |
| `{{风险}}` | `{{措施和证据}}` | `{{姓名或角色}}` |

- 可并行任务：`{{编号及互不重叠的文件/接口边界}}`
- 不可并行区域：`{{共享配置、迁移、公开 API 或安全边界}}`
- 第二检查点：`{{Worker 验证和人工审查的安排}}`

## 7. 人工批准

- [ ] 我已逐条检查任务、依赖、文件边界和回滚方案。
- [ ] 我确认规格与计划一致，且没有隐含的新需求。
- [ ] 我确认风险车道和审查角色匹配。
- 批准人：`{{姓名或角色}}`
- 批准日期：`{{YYYY-MM-DD}}`

