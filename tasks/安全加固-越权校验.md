# 任务简报：越权校验加固（患者端数据权限）

## 基本信息

- 负责人：本人（AI 辅助）
- 日期：2026-08-23
- 关联：AGENTS.md 关键不变量"患者数据权限"；盘点差距清单 #1
- 风险车道：**红区**（鉴权/授权逻辑）
- AI 使用方式：委托代理

## 目标与边界

**目标**

1. **网关 PATIENT 白名单**（AuthGlobalFilter）：患者角色仅可访问患者面路径——`/api/portal/**`、`/api/patient/archives/my`、`/api/followup/plans/{id}`、`/api/followup/patients/**`、`/api/ai/chat|report/interpret`、`/api/notify/messages/**`、`/api/auth/logout|me|refresh`；其余一律 403。
2. **服务侧归属校验**：PATIENT 调含 patientId 的患者侧读/写接口时，patientId 必须等于本人档案（经 `PatientApi.getArchiveByUserId` 解析）。
3. **员工专属接口拒患者**：工作台/规则/报告抽取确认/建档/统计等在服务层显式拒 PATIENT。
4. **内部契约放行**：`/api/*/internal/**` 不跑归属校验（服务间调用；AI 工具已强制当前患者）。
5. 新增单测：网关白名单放行/拦截、归属校验通过/拒绝/员工放行/系统上下文放行。

**不在范围**：菜单/用户 CRUD（P1b）、操作审计日志、Flyway。

## 实现路径

- PatientApi 加 `getArchiveByUserId(userId)`（黄区契约补充）
- followup/nodule 各放一个 `PatientDataGuard`（读 X-User-* 头；PATIENT→解析本人档案比对；无请求上下文（Job/MQ）放行）
- patient 服务自查（detail 归属 + 员工接口拒患者）

## 验收标准

- [ ] 患者 token 调他人计划 → 403；调本人 → 通过；员工不受限
- [ ] 患者访问 /api/stats/**、规则维护、AI 知识库导入 → 403
- [ ] AI 工具链（X-User 透传的内部调用）不被误伤
- [ ] 全量构建 + 测试全绿

## 开工批准

- 审阅者：本人（用户指示"先补越权校验"）
- 日期：2026-08-23
