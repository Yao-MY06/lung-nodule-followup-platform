# 任务简报：P2 核心域（patient / nodule / followup 随访引擎）

## 基本信息

- 负责人：本人（AI 辅助）
- 日期：2026-08-22
- 关联需求：specs/README.md §落地顺序 P2；modules/patient.md、nodule.md、followup.md；flows/F3
- 风险车道：**黄区**（跨模块、数据库、Feign 契约）
- AI 使用方式：委托代理

## 目标与边界

**目标**

1. 三个服务模块编译通过 + 核心单测：patient（8082）、nodule（8083）、followup（8084）。
2. **随访规则引擎（表驱动）**：`decision_rule.cond_json` 条件求值 + 优先级选择 + 模板匹配，无 if-else 硬编码间隔；种子规则固化需求分析报告 §3.2/§3.3 指南表。
3. 计划生成：首查/重复间隔铺任务时间轴至总年限；Redisson 防重 + 唯一索引兜底；工作台分页查询；任务完成流转；重新生成（改规则即时生效演示路径）。
4. 结节快照对比：最近两次快照 Δ直径/实性成分，≥2mm 判进展（F3 判定逻辑，MQ 事件 P3 接入）。
5. 建档联动：patient 建档成功后 Feign 调 followup 自动生成计划（失败不阻断建档，P4 Seata 强化）。
6. init-sql 三库 DDL + followup 规则/模板种子。

**不在范围内**

- MQ 事件发布/消费、XXL-Job 扫描（P3）；AI 抽取、Seata（P4）；患者全景时间轴聚合接口（P2b）；MDT；lab_result 业务代码（仅建表）。

## 上下文入口

- 契约变更（黄区）：yiliao-api 新增 PatientApi/NoduleApi/FollowupApi 及 DTO
- 决策：新表统一补 create_by/update_by 列（对齐 specs/global/20 §1 BaseEntity）；nodule 对比逻辑与升级决策分离（nodule 算 Δ，followup 定策略）

## 验收标准

- [ ] `mvn install` 全绿（14 模块）
- [ ] 规则引擎单测：条件运算符（≤/≥/区间/布尔/枚举）、优先级、无命中报 22002
- [ ] 计划生成单测：间隔铺点正确、总年限截断、防重、任务数上限保护
- [ ] 对比单测：≥2mm 触发/不触发、无基线返回
- [ ] 种子 SQL 幂等，规则表覆盖 §3.2/§3.3 主要分支

## 开工批准

- 计划审阅者：本人（延续用户"继续"指示）
- 批准日期：2026-08-22
