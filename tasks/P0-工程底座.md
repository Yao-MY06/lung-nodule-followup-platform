# 任务简报：P0 工程底座

## 基本信息

- 负责人：本人（AI 辅助）
- 日期：2026-08-22
- 关联需求：specs/README.md §落地顺序 P0
- 风险车道：**绿区**（纯骨架：POM 结构、无业务逻辑的公共类、中间件编排；不含 JWT/鉴权实现——那是 P1 红区任务）
- AI 使用方式：委托代理

## 目标与边界

**目标**

1. Maven 单仓多模块骨架可构建：`mvn install` 全绿（parent + 6 个 common + yiliao-api）。
2. 版本基线锁定（Boot 3.4.5 / SC 2023.0.3 / SCA 2023.0.3.2，依据 specs/global/30 §1）。
3. docker-compose 可启动核心中间件（MySQL/Redis/Nacos/RocketMQ/MinIO），初始化 7 个业务库。
4. 落地 specs 全局文档中已定稿的基础类：Result/ErrorCode/BizException/PageQuery/PageResult/BaseEnum/BaseEvent/Topics/BaseEntity/全局异常处理/Feign 透传拦截器。

**不在范围内**

- 9 个服务模块（P1 起）、任何业务表 DDL（随阶段入 init-sql）、JWT/安全实现（P1 红区）、Spring AI/Redisson/RocketMQ 的实际使用代码（仅锁版本）。

**受影响区域**

- 新增：`pom.xml`、`yiliao-common/**`、`yiliao-api/**`、`docker/**`、`.gitignore`
- 不触碰：`specs/`、`make/`、既有工作流文件

## 上下文入口

- 事实源：specs/global/10（返回体/错误码）、specs/global/20（枚举/基类/事件）、specs/global/30（版本/结构/依赖规则）
- 实现决策：JDK25 环境暂不引 Lombok（版本兼容未验证），骨架类全用 record/手写访问器

## 验收标准

- [ ] 根目录 `mvn install` BUILD SUCCESS（全部 8 个模块）
- [ ] `docker compose up -d` 后 MySQL/Redis/Nacos/RocketMQ/MinIO 容器 healthy/running，7 个库已建
- [ ] parent POM dependencyManagement 锁定全部版本，子 POM 无版本号
- [ ] common 之间依赖只指向 common-core；api 只依赖 common-core+openfeign（结构规则 G30 §3）

## 路径选择

- [x] 绿区小任务，使用轻量计划（本简报即计划，实现顺序：parent → core → web → data → security → mq → feign → api → docker）

## 开工批准

- 计划审阅者：本人（用户指示"开工"）
- 批准日期：2026-08-22
- 备注：完成后出验证报告 tasks/P0-工程底座-验证报告.md
