# 验证报告：P0 工程底座

- 任务简报：`tasks/P0-工程底座.md`
- 风险车道：绿区
- 验证人：本人（AI 辅助实现 + 本地执行验证）
- 日期：2026-08-22

## 自动化检查

| 检查 | 命令 | 结果 | 证据摘要 |
|---|---|---|---|
| 编译安装 | `mvn install` | **通过** | Reactor 9 项全 SUCCESS（parent/common×7/api），Total time 3.3s |
| 单元测试 | `mvn test` | **通过** | 11 个用例全绿：core 7（Result/PageQuery/PageResult）、web 4（GlobalExceptionHandler：业务异常/参数校验/404/兜底脱敏） |
| 版本锁定 | parent POM `dependencyManagement` | 通过 | Boot 3.4.5 / SC 2023.0.3 / SCA 2023.0.3.2 / Spring AI 1.0.0 / MP 3.5.7 / Redisson 3.37.0 / RocketMQ-starter 2.3.1 / Knife4j 4.5.0 / jjwt 0.12.6；子 POM 均无版本号 |
| 结构规则 | 依赖检查 | 通过 | common 间依赖仅指向 common-core；api 仅 core+openfeign；无服务间依赖（当前尚无服务模块） |

## 手工验证

| 场景 | 预期 | 实际 | 结论 |
|---|---|---|---|
| 中间件启动 | compose up 后 5 容器健康、7 库建立 | **未验证（挂起）** | compose 配置校验通过（`docker compose config --quiet` OK）；Docker daemon 未运行，按用户指示暂缓启动。P1 运行链路验证前需补 |
| 构建产物 | 本地仓库可被服务模块引用 | 通过 | `mvn install` 落库 com.yiliao:1.0.0-SNAPSHOT |

## 失败与处理

- 编译失败 ×2，均已修复：① record 访问器与 ErrorCode 接口方法不匹配（SimpleErrorCode 显式实现 getMsg）；② common-feign 缺 servlet-api（补 provided 依赖）。
- 实现期修正 ×2：@JsonComponent 嵌套类不会被自动配置注册 → 改 Jackson2ObjectMapperBuilderCustomizer；SecurityContextHolder 曾引用未依赖的 spring-web → 移除。

## 决策记录（偏离/取舍）

1. **未引入 Lombok**：本机 JDK 25，Lombok 兼容版本未验证；P0 骨架全用 record/手写访问器。P1 验证 1.18.4x 后再决定是否引入。
2. **PageQuery 校验注解测试**移除：core 模块保持零依赖（引 hibernate-validator 只为测试不值当），注解生效由 web 层 starter-validation 保证。
3. Redis/Redisson/RocketMQ/Spring AI 仅锁版本未写实现代码，按阶段落地（G30 对齐）。

## 结论

- [x] 适用检查通过，证据可复现（命令见上）
- [x] 无硬编码凭据（docker 密码仅本地开发环境默认值）；compose 中 xxl-job/seata/sentinel 为注释占位
- [ ] 中间件启动验证挂起 → **P0 代码部分完成；docker 项作为遗留项移交 P1 验证时一并执行**

结论：P0 可进入人工审查（遗留项已登记）
