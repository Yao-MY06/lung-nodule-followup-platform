# 验证报告：P5 呈现与治理（statistics / file / Sentinel / 压测准备）

- 任务简报：`tasks/P5-呈现治理.md`
- 风险车道：绿区为主 + gateway 依赖变更（黄区）
- 验证人：本人（三子代理并行实现 + 主线集成验证）
- 日期：2026-08-23

## 自动化检查

| 检查 | 命令 | 结果 | 证据摘要 |
|---|---|---|---|
| 全量构建 | `mvn install` | **通过** | Reactor 18 项全 SUCCESS（新增 statistics/file；gateway +sentinel） |
| statistics 单测 | `StatsCalculatorTest`(8) + `StatsAggregateServiceTest`(6) | **通过** | 指标口径（完成率/及时率、4 位小数、除零、边界）；快照重建先删后插（InOrder）、空集合不插、空表返回零值、月度升序 |
| file 单测 | `FileServiceTest`(6) | **通过** | 合法上传对象键格式正则断言、exe→26001、超 100MB→26002、`..` 穿越→26003、非法前缀→26003、合法下载 1800s |
| gateway 回归 | `AuthGlobalFilterTest`(7) | **通过** | Sentinel 依赖接入未破坏既有过滤器行为 |

**全项目累计 126 个测试通过（18 模块）。**

## 子代理产出人工抽查记录

- **file**：FileApi 契约遵守绝对路径约定；FileService 校验链完整（后缀/大小/bizType 白名单/路径穿越/前缀），MinIO 异常转译、日志不记完整签名 URL——符合 specs。子代理自行补充"上传侧校验 bizType"为合理安全决策。
- **statistics**：DDL 快照表取舍（**物理 DELETE 重建**而非逻辑删除——逻辑删除会与唯一键在重建时冲突）已在 SQL 头注释说明理由，快照属可再生预聚合数据，判断正确；Demo 数据分布合计自洽（毕设演示数据，P5b 换 Feign 真实数据，specs §4.1 演进路径保留）。
- **治理文档**：子代理 C 核对了演示步骤与真实代码（24001/24002 降级码、种子账号、环境变量故障注入），并如实报告两处边界——其中"网关缺 BlockException→11001 转换"由主线补齐（见下），"网关流控专用 Tab 需适配器"以普通流控规则满足验收，可接受。

## 主线集成改动

- 新增 `SentinelBlockHandlerConfig`：`WebFluxCallbackManager.setBlockHandler` 注册统一限流响应 `429 + Result{code:11001}`（specs/modules/gateway.md §4.2，javap 核实适配器接口签名后实现）。

## 手工验证

| 场景 | 预期 | 实际 | 结论 |
|---|---|---|---|
| 驾驶舱四接口（空库/有快照） | JSON 聚合返回 | **未验证（挂起）** | 需 MySQL 运行；空表行为已有 6 个单测覆盖 |
| Sentinel 限流 11001 / LLM 熔断降级 / JMeter 三档压测 | 演示证据 | **未验证（挂起）** | 完整可执行步骤已写入 `tasks/P5-熔断与压测方案.md`（含环境变量故障注入法），中间件就绪后照做即可 |

## 结论

- [x] P0~P5 全部阶段代码完成：**9 服务 + 6 common + api，18 模块，126 测试全绿**
- [x] 论文证据四件套的采集路径全部就绪（消融→P4b、压测/熔断→P5 方案文档、幂等→P3 报告）
- [ ] 运行时验证统一挂起（Docker + LLM Key），是当前唯一阻塞项

结论：P5 可进入人工审查；项目进入"运行时验证 + P4b/P5b 深化"阶段
